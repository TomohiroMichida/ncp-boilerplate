/**
 * jest.setup.ts
 *
 * This file runs once before all tests start and can be used
 * to set up global mocks, polyfills, or test environment settings.
 * It is intentionally left empty in this template because
 * no global setup is needed at the moment.
 */
